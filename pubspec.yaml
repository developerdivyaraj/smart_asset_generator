name: smart_asset_generator
description: A Dart/Flutter CLI toolkit for generating asset references, barrel files, GetX modules, and GitLab PR checker scaffolding.
version: 0.1.0
homepage: https://github.com/developerdivyaraj/smart_asset_generator
repository: https://github.com/developerdivyaraj/smart_asset_generator

environment:
  sdk: ^3.7.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.5.0
  yaml: ^3.1.3
  path: ^1.9.1
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

# Fix the executables section - both should match
executables:
  smart_asset_generator: smart_asset_generator

flutter: